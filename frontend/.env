# Base URL of the FastAPI backend (main.py). No trailing slash.
# This is the ONLY place the backend URL is configured — there is
# deliberately no editable "Backend URL" field anywhere in the UI.
VITE_API_BASE_URL=http://localhost:8000
